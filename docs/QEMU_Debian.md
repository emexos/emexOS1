# How to install QEMU in Debian-based linux distributions

You don't need multiple dependencies to run QEMU in your debian-based distribution. 
```
sudo apt install bridge-utils virt-manager
```
[Check this post](https://forums.linuxmint.com/viewtopic.php?t=428069) in Linux Mint Forums to understand why we don't need to install a huge load of dependencies.