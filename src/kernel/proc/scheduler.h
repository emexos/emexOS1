#ifndef SCHEDULER_H
#define SCHEDULER_H

#include "../../../shared/types.h"

// Basic scheduler functions (not used yet)
void scheduler_init(void);
void scheduler_tick(void);
int scheduler_is_enabled(void);

#endif
